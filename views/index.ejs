<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>

    <ul>
        <% blogsList.forEach((blog, index) => { %>
            <li><a href="/blog/<%=index%>" ><%= blog.title %></a></li>
        <% }); %>
    </ul>
</body>
</html>